from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A ml project to predict the mental health score on the scale of 1 to 10',
    author='aayush',
    license='',
)
